'use client';
import Main from './(internal)/main/main';

export default function Page() {
    /* useEffect(() => {
        window.location.href = TASK_ECOCLIM_MAIN!;
    }, []); */
    return <Main />;
}
